- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."article"("id" serial NOT NULL, "title" text NOT NULL,
      "content" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "author_id"
      integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("author_id") REFERENCES
      "public"."user"("id") ON UPDATE restrict ON DELETE restrict);
  type: run_sql
- args:
    name: article
    schema: public
  type: add_existing_table_or_view
